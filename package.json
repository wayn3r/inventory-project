{
  "name": "inventory_project",
  "version": "1.0.0",
  "description": "Inventory project for class ISO815",
  "main": "index.js",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "test": "jest",
    "build": "rm -rf apps/server/public && npm run build -w web && mv apps/web/dist apps/server/public && npm run build -w server",
    "start": "npm run start:prod -w server"
  },
  "author": "Wayner",
  "license": "ISC"
}
